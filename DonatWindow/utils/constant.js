import { formatDate } from './dateFormat';

export const date = formatDate(new Date());

export const totalAmout = document.querySelector('#total-amount');

export const submitButton = document.querySelector('.donate-form__submit-button');

// export const totalAmoutContent = 28;
